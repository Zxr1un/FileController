<Window x:Class="FileController.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileController"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Путь -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0 0 0 10">
            <TextBox x:Name="PathBox" Width="500" Margin="0 0 10 0"/>
            <Button Content="Выбрать" Click="SelectFolder_Click"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0 0 0 10">
            <Button Content="Создать репозиторий" Click="CreateRepo_Click" Margin="0 0 10 0"/>
            <Button Content="Открыть репозиторий" Click="OpenRepo_Click"/>
        </StackPanel>

        <!-- Коммиты -->
        <ListBox x:Name="CommitsList" Grid.Row="2" DisplayMemberPath="Name"/>

        <!-- Управление -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0 10 0 0">
            <TextBox x:Name="CommitNameBox" Width="200" Margin="0 0 10 0" Text="Имя коммита"/>
            <Button Content="Commit" Click="Commit_Click" Margin="0 0 10 0"/>
            <Button Content="Load Commit" Click="LoadCommit_Click"/>
        </StackPanel>
    </Grid>
</Window>
